{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user profile in the application.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user's unique ID." },
        "email": { "type": "string", "format": "email", "description": "The user's email address." },
        "username": { "type": "string", "description": "The user's chosen username." },
        "displayName": { "type": "string", "description": "The user's display name." },
        "photoURL": { "type": "string", "description": "The URL of the user's profile photo." },
        "fcmTokens": { "type": "array", "items": { "type": "string" }, "description": "Array of FCM tokens for push notifications." },
        "isVerified": { "type": "boolean", "description": "Whether the user is verified." },
        "following": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs this user follows." },
        "followers": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs following this user." },
        "mutedUsers": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs this user has muted." },
        "blockedUsers": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs this user has blocked." },
        "bookmarks": { "type": "array", "items": { "type": "string" }, "description": "Array of post IDs this user has bookmarked." },
        "pinnedPostIds": { "type": "array", "items": { "type": "string" }, "description": "Array of post IDs pinned to the user's profile." },
        "mutedWords": { "type": "array", "items": { "type": "string" }, "description": "List of words or hashtags to filter from the feed." },
        "profileTheme": { "type": "string", "description": "CSS or identifier for personal profile styling." },
        "lastSeen": { "type": "timestamp", "description": "Last time the user was active." },
        "activeMissions": { "type": "array", "items": { "type": "string" }, "description": "Current daily missions." },
        "points": { "type": "number", "description": "The user's earned points." },
        "level": { "type": "number", "description": "The user's current level." },
        "badges": { "type": "array", "items": { "type": "string" }, "description": "Array of earned badge IDs." },
        "createdAt": { "type": "timestamp", "description": "The time the user was created." }
      },
      "required": ["uid", "email", "username", "displayName"]
    },
    "Post": {
      "title": "Post",
      "description": "A post created by a user.",
      "type": "object",
      "properties": {
        "content": { "type": "string", "description": "The text content of the post." },
        "authorId": { "type": "string", "description": "The UID of the user who created the post." },
        "authorName": { "type": "string", "description": "The display name of the author." },
        "authorUsername": { "type": "string", "description": "The username of the author." },
        "authorPhoto": { "type": "string", "description": "The photo URL of the author." },
        "authorVerified": { "type": "boolean", "description": "Whether the author is verified." },
        "createdAt": { "type": "timestamp", "description": "The time the post was created." },
        "likesCount": { "type": "number", "description": "The number of likes the post has received." },
        "repliesCount": { "type": "number", "description": "The number of replies the post has received." },
        "repostsCount": { "type": "number", "description": "The number of reposts the post has received." },
        "replyToId": { "type": "string", "description": "The ID of the post being replied to." },
        "replyToUsername": { "type": "string", "description": "The username of the author being replied to." },
        "replyToVerified": { "type": "boolean", "description": "Whether the author being replied to is verified." },
        "likes": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs who liked the post." },
        "reposts": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs who reposted the post." },
        "hashtags": { "type": "array", "items": { "type": "string" }, "description": "List of hashtags extracted from content." },
        "scheduledFor": { "type": "timestamp", "description": "Time to publish (Premium)." },
        "communityId": { "type": "string", "description": "The community this post belongs to." }
      },
      "required": ["content", "authorId", "authorName", "authorUsername", "createdAt", "likesCount", "repliesCount", "repostsCount"]
    },
    "Community": {
      "title": "Community",
      "description": "A group based on interests.",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "slug": { "type": "string" },
        "description": { "type": "string" },
        "bannerUrl": { "type": "string" },
        "memberCount": { "type": "number" },
        "members": { "type": "array", "items": { "type": "string" } },
        "moderators": { "type": "array", "items": { "type": "string" } },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["name", "slug", "createdAt"]
    },
    "Report": {
      "title": "Report",
      "description": "A moderation report.",
      "type": "object",
      "properties": {
        "reporterId": { "type": "string" },
        "targetId": { "type": "string", "description": "Post ID or User ID being reported." },
        "targetType": { "type": "string", "enum": ["post", "user", "comment"] },
        "reason": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "resolved", "dismissed"] },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["reporterId", "targetId", "targetType", "reason", "status", "createdAt"]
    },
    "Mission": {
      "title": "Mission",
      "description": "A daily engagement goal.",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "type": { "type": "string", "enum": ["like", "repost", "reply", "post"] },
        "requirement": { "type": "number", "description": "Count needed to complete." },
        "reward": { "type": "number", "description": "Points bestowed upon completion." },
        "createdAt": { "type": "timestamp" }
      },
      "required": ["title", "type", "requirement", "reward", "createdAt"]
    },
    "Conversation": {
      "title": "Conversation",
      "description": "A direct message conversation between two users.",
      "type": "object",
      "properties": {
        "participants": { "type": "array", "items": { "type": "string" }, "description": "Array of UIDs participating in the conversation." },
        "participantInfo": { "type": "object", "description": "Metadata for each participant." },
        "lastMessage": { "type": "string", "description": "The text of the most recent message." },
        "lastMessageSenderId": { "type": "string", "description": "The UID of the user who sent the last message." },
        "unreadCount": { "type": "object", "description": "Map of userId to unread message count." },
        "typing": { "type": "object", "description": "Map of userId to boolean typing status." },
        "updatedAt": { "type": "timestamp", "description": "The time of the last update." },
        "createdAt": { "type": "timestamp", "description": "The time the conversation was created." }
      },
      "required": ["participants", "participantInfo", "updatedAt", "createdAt"]
    },
    "Message": {
      "title": "Message",
      "description": "A message within a conversation.",
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "The text content of the message." },
        "senderId": { "type": "string", "description": "The UID of the user who sent the message." },
        "createdAt": { "type": "timestamp", "description": "The time the message was sent." },
        "isDeleted": { "type": "boolean", "description": "Whether the message has been deleted." },
        "read": { "type": "boolean", "description": "Whether the message has been read by the recipient." }
      },
      "required": ["text", "senderId", "createdAt"]
    },
    "Notification": {
      "title": "Notification",
      "description": "A notification sent to a user.",
      "type": "object",
      "properties": {
        "type": { "type": "string", "enum": ["like", "follow", "reply"], "description": "The type of notification." },
        "recipientId": { "type": "string", "description": "The UID of the user receiving the notification." },
        "senderId": { "type": "string", "description": "The UID of the user who triggered the notification." },
        "senderName": { "type": "string", "description": "The display name of the sender." },
        "senderUsername": { "type": "string", "description": "The username of the sender." },
        "senderPhoto": { "type": "string", "description": "The photo URL of the sender." },
        "senderVerified": { "type": "boolean", "description": "Whether the sender is verified." },
        "postId": { "type": "string", "description": "The ID of the post related to the notification." },
        "content": { "type": "string", "description": "The content of the notification (e.g., reply text)." },
        "read": { "type": "boolean", "description": "Whether the notification has been read." },
        "createdAt": { "type": "timestamp", "description": "The time the notification was created." }
      },
      "required": ["type", "recipientId", "senderId", "senderName", "senderUsername", "read", "createdAt"]
    },
    "Ad": {
      "title": "Ad",
      "description": "An advertisement to be displayed in the feed.",
      "type": "object",
      "properties": {
        "authorName": { "type": "string", "description": "The display name of the advertiser." },
        "authorUsername": { "type": "string", "description": "The username of the advertiser." },
        "authorPhotoURL": { "type": "string", "description": "The photo URL of the advertiser." },
        "content": { "type": "string", "description": "The text content of the ad." },
        "imageUrl": { "type": "string", "description": "The URL of the attached image." },
        "link": { "type": "string", "description": "The destination URL when the ad is clicked." },
        "ctaText": { "type": "string", "description": "The call to action text." },
        "active": { "type": "boolean", "description": "Whether the ad is currently active." },
        "createdAt": { "type": "timestamp", "description": "The time the ad was created." }
      },
      "required": ["authorName", "authorUsername", "content", "link", "active", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles."
    },
    "/posts/{postId}": {
      "schema": { "$ref": "#/entities/Post" },
      "description": "Stores posts created by users."
    },
    "/conversations/{conversationId}": {
      "schema": { "$ref": "#/entities/Conversation" },
      "description": "Stores direct message conversations."
    },
    "/conversations/{conversationId}/messages/{messageId}": {
      "schema": { "$ref": "#/entities/Message" },
      "description": "Stores messages within a conversation."
    },
    "/notifications/{notificationId}": {
      "schema": { "$ref": "#/entities/Notification" },
      "description": "Stores user notifications."
    },
    "/ads/{adId}": {
      "schema": { "$ref": "#/entities/Ad" },
      "description": "Stores advertisements."
    },
    "/communities/{communityId}": {
      "schema": { "$ref": "#/entities/Community" },
      "description": "Stores interest-based groups."
    },
    "/reports/{reportId}": {
      "schema": { "$ref": "#/entities/Report" },
      "description": "Stores moderation reports."
    },
    "/missions/{missionId}": {
      "schema": { "$ref": "#/entities/Mission" },
      "description": "Stores daily goals."
    }
  }
}
